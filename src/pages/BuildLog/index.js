import React from 'react'

import MainContent from '../../components/layout/MainContent';

const BuildLog = () => (
  <MainContent title="Build Log">
      This is where the Build Log goes..
  </MainContent>
)
export default BuildLog
